import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    borderWidth: 0,
    height: 50,    
    fontSize: 16,
    marginTop: 5,
    color: '#000000'
  },
  label: {
    fontSize: 12,
    color: '#000000',
    position: 'absolute'
  }
});